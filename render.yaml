# render.yaml — Render.com Blueprint for QS-VC
# Deploy: Connect this repo on https://dashboard.render.com/blueprints
# Docs: https://docs.render.com/infrastructure-as-code

databases:
  - name: qsvc-db
    plan: free
    databaseName: qsvc
    user: qsvc
    ipAllowList: []  # Allow all (required for free tier)

services:
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # AUTH SERVICE
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - type: web
    name: qsvc-auth
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./services/auth/Dockerfile
    dockerContext: ./services/auth
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: qsvc-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRY
        value: "24h"
      - key: TURN_SECRET
        value: demo-turn-secret
      - key: TURN_SERVERS
        value: "stun:stun.l.google.com:19302"
      - key: CORS_ORIGINS
        value: "https://qs-vc.vercel.app,https://dist-puce-one-68.vercel.app,http://localhost:5173"

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # SIGNALING SERVICE
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - type: web
    name: qsvc-signaling
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./services/signaling/Dockerfile
    dockerContext: ./services/signaling
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        sync: false  # Must match auth service
      - key: WEB_URL
        value: "https://qs-vc.vercel.app"
      - key: CORS_ORIGINS
        value: "https://qs-vc.vercel.app,https://dist-puce-one-68.vercel.app,http://localhost:5173"

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # MEETING SERVICE
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - type: web
    name: qsvc-meeting
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./services/meeting/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: qsvc-db
          property: connectionString
      - key: JWT_SECRET
        sync: false  # Must match auth service
      - key: WEB_URL
        value: "https://qs-vc.vercel.app"
      - key: CORS_ORIGINS
        value: "https://qs-vc.vercel.app,https://dist-puce-one-68.vercel.app,http://localhost:5173"
